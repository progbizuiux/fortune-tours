// Fonts are now loaded locally via @font-face in app/design-system.css
// based on the Fortune Tours typography foundation.
// We export these objects to maintain compatibility with app/layout.js
// which expects a `.variable` property to apply class names.

export const spartan = {
  variable: "font-spartan",
};

export const poppins = {
  variable: "font-poppins",
};
